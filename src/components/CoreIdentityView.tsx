import React,{useCallback,useEffect,useState}from'react';import{RefreshCw,ShieldCheck,Users}from'lucide-react';import{getCoreApiUrl}from'../lib/coreApi';
export const CoreIdentityView:React.FC<{showToast?:(msg:string)=>void}>=({showToast=()=>{}})=>{const[items,setItems]=useState<any[]>([]);const[privileged,setPrivileged]=useState(false);const[loading,setLoading]=useState(false);const load=useCallback(async()=>{setLoading(true);try{const r=await fetch(`${getCoreApiUrl()}/api/v1/product/identities?privileged=${privileged}`,{credentials:'include'});const b=await r.json();if(!r.ok)throw new Error(b?.detail||'Gagal mengambil identity posture.');setItems(b.items||[])}catch(e){showToast(e instanceof Error?e.message:'Gagal mengambil identity posture.')}finally{setLoading(false)}},[privileged,showToast]);useEffect(()=>{void load()},[load]);return <div id="identity-view" className="flex-1 overflow-y-auto bg-[#f8fafc] dark:bg-slate-950 px-4 sm:px-8 py-6 max-w-7xl mx-auto w-full space-y-6"><div className="flex justify-between gap-3"><div><div className="text-[11px] uppercase font-bold tracking-wider text-slate-500 font-mono">NusaSec Identity Posture</div><h1 className="text-xl sm:text-2xl font-bold text-slate-900 dark:text-white">Identity &amp; Access Explorer</h1><p className="text-xs sm:text-sm text-slate-500">Identity signals dihitung dari asset metadata pada scan terbaru Core.</p></div><div className="flex gap-2"><button onClick={()=>void load()} className="p-2.5 rounded-xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800"><RefreshCw className={loading?'w-4 h-4 animate-spin':'w-4 h-4'}/></button><button onClick={()=>setPrivileged(v=>!v)} className={`px-3 py-2 rounded-xl text-xs font-semibold ${privileged?'bg-rose-600 text-white':'bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 text-slate-700 dark:text-slate-200'}`}><Users className="w-4 h-4 inline mr-1"/> Privileged only</button></div></div><div className="space-y-3">{items.map(x=><div key={x.external_id} className="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl p-4 flex justify-between gap-3"><div><div className="font-semibold text-sm text-slate-900 dark:text-white">{x.identity}</div><div className="text-xs text-slate-500 mt-1">{x.provider} • {x.asset_type} • {x.external_id}</div></div><div className="flex gap-2 text-[11px]"><span className={`px-2 py-1 rounded ${x.privileged?'bg-rose-50 text-rose-700':'bg-slate-100 text-slate-600'}`}>{x.privileged?'PRIVILEGED':'STANDARD'}</span><span className="px-2 py-1 rounded bg-blue-50 text-blue-700">MFA {x.mfa_enabled==null?'UNKNOWN':x.mfa_enabled?'ON':'OFF'}</span></div></div>)}</div>{!items.length&&<div className="text-sm text-slate-400 text-center p-8">Belum ada identity signal dari scan.</div>}</div>};
